{
  "name": "iot-presence-playlist-player",
  "version": "1.0.0",
  "description": "IoT-based zonal music system with presence detection using phones",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../sensor-app && npm install && cd ../main-app && npm install",
    "start:backend": "cd backend && npm start",
    "start:sensor": "cd sensor-app && npm start",
    "start:main": "cd main-app && npm start",
    "dev": "concurrently \"npm run start:backend\" \"npm run start:sensor\" \"npm run start:main\"",
    "build:main": "cd main-app && npm run build",
    "build:sensor": "cd sensor-app && npm run build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  },
  "keywords": [
    "iot",
    "presence-detection",
    "music",
    "mqtt",
    "frigate",
    "react-native"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

